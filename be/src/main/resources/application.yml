spring:
  application:
    name: CarRentalSystem
  profiles:
    active: dev

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:ev_fleet}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    driverClassName: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME:evfleet}
    password: ${DB_PASSWORD:evfleet123}
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:validate}
    show-sql: ${JPA_SHOW_SQL:true}
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  flyway:
    enabled: ${FLYWAY_ENABLED:true}
    baseline-on-migrate: ${FLYWAY_BASELINE_ON_MIGRATE:true}
    locations: classpath:db/migration
    schemas: ${DB_NAME:ev_fleet}

# Auth configuration
app:
  auth:
    jwt:
      secret: ${JWT_SECRET:ev-fleet-secret-key-minimum-32-characters-required-for-hs256}
      expiration-ms: ${JWT_EXPIRATION_MS:3600000}
    refresh-token:
      expiration-days: ${REFRESH_TOKEN_EXPIRATION_DAYS:7}

logging:
  level:
    org.hibernate.SQL: ${LOG_LEVEL_SQL:info}
    org.hibernate.type.descriptor.sql.BasicBinder: ${LOG_LEVEL_BINDER:trace}
    org.flywaydb: ${FLYWAY_LOG_LEVEL:info}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
